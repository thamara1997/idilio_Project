package com.idilio.backend.repository;

import com.idilio.backend.entity.Payment2;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Payment2Repo extends JpaRepository<Payment2,Integer> {
}
